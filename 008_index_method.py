# Create a list with repeated names elements
names = ['Alice', 'Bob', 'Charlie', 'Alice', 'Bob']

# Get the index number of Bob
bob_index = names.index('Bob')
print("Bob's index:", bob_index)

# Get the index number of Alice
alice_index = names.index('Alice')
print("Alice's index:", alice_index)